modules:
- type: files
  files:
    - source: dconf-ublue
      destination: /etc/dconf/db/distro.d
- type: script
  scripts:
    - ublue-get-files-from-bluefin.sh
- type: dnf
  repos:
    copr:
      - ublue-os/staging
  install:
    packages:
      - gnome-shell-extension-logo-menu


